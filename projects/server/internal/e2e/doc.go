// Package e2e contains integration tests that wire the real gateway, ingress
// and registry together with a real WebSocket agent.
//
// It has no non-test code on purpose: it exists to prove the packages compose,
// not to be imported by them.
package e2e
