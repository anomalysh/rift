{
  "name": "tunl",
  "version": "0.1.0",
  "description": "Fully-typed Bun/TypeScript single-binary ngrok-style tunnel agent.",
  "type": "module",
  "bin": {
    "tunl": "./dist/tunl"
  },
  "scripts": {
    "build": "bun build --compile --minify --sourcemap ./src/index.ts --outfile dist/tunl",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "bun-types": "^1.3.12",
    "typescript": "^5.5.0"
  }
}
